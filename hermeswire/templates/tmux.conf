# HermesWire recommended tmux configuration
# Install: hermeswire init (or copy to ~/.tmux.conf)
# Reload: prefix + r (or: tmux source-file ~/.tmux.conf)

# ─────────────────────────────────────────────────────────────
# Core Settings
# ─────────────────────────────────────────────────────────────

# Enable mouse support — scroll through agent output, click panes, resize
set -g mouse on

# Focus events — lets a terminal agent detect focus changes. Kept on as a
# harmless default; Hermes doesn't require it, but focus-aware TUIs may.
set -g focus-events on

# Large scrollback buffer — agent sessions produce a lot of output
set -g history-limit 50000

# Windows and panes both start at 0.
# Pane 0 = orchestrator convention is used by hermeswire hooks, and keeping the
# window axis on the same base means a session's agent is at `<session>:0.0`
# under this config — one less place for an off-by-one to hide.
#
# NOTE for hermeswire code: this is a DEFAULT, never a guarantee. A user may set
# their own base-index, and windows created before a change keep their old
# index regardless. Resolve a session's real pane (e.g. the first row of
# `tmux list-panes -t <session> -F '#{window_index}.#{pane_index}'`) instead of
# hardcoding an index — see #901, where a hardcoded `:0.0` failed against a
# base-index-1 machine.
set -g base-index 0
setw -g pane-base-index 0

# Renumber windows when one is closed (no gaps)
set -g renumber-windows on

# Use largest connected client size — prevents a small attached client
# (e.g. the portal Monitor view) from shrinking windows for everyone
set -g window-size largest

# Re-assert largest on attach and new-session — explicit resize-window calls
# (portal terminal, `hermeswire resize`) flip a window to "manual" size mode;
# these hooks flip it back to automatic
set-hook -g client-attached 'set-option -t "#{session_name}" window-size largest'
set-hook -g after-new-session 'set-option window-size largest'

# No delay after pressing Escape (important for vi-mode users)
set -s escape-time 0

# True color support
set -g default-terminal "screen-256color"
set -ga terminal-overrides ",xterm-256color:Tc"

# ─────────────────────────────────────────────────────────────
# Copy Mode (vi-style)
# ─────────────────────────────────────────────────────────────

# Use vi keys in copy mode (prefix + [ to enter)
setw -g mode-keys vi

# v to start selection, y to yank (like vim)
bind -T copy-mode-vi v send -X begin-selection
bind -T copy-mode-vi y send -X copy-selection-and-cancel

# Don't exit copy mode when mouse drag ends — lets you review the selection.
# Unbind in BOTH tables: a stray click-drag can drop the pane into the default
# copy-mode table (not just copy-mode-vi), where a lingering selection wedges
# the portal's chunked WebSocket-terminal paste (writes chunked 512B/10ms).
unbind -T copy-mode-vi MouseDragEnd1Pane
unbind -T copy-mode    MouseDragEnd1Pane

# Selection-aware mouse wheel in copy mode (governs tablet two-finger scroll too:
# the portal converts touch swipes into real SGR wheel sequences to the PTY).
# No selection → scroll-up/down keeps smooth view scrolling (a plain cursor-up
# binding makes the cursor walk ~a full screen before the view moves — a
# dead-zone that feels broken on touch). Active selection → cursor-up/down moves
# the selection end so the highlight grows and the view auto-scrolls at the edge
# (tmux's only real "grow selection while scrolling").
bind -T copy-mode-vi WheelUpPane   if -F '#{selection_present}' 'send -X -N 3 cursor-up'   'send -X -N 3 scroll-up'
bind -T copy-mode-vi WheelDownPane if -F '#{selection_present}' 'send -X -N 3 cursor-down' 'send -X -N 3 scroll-down'

# ─────────────────────────────────────────────────────────────
# Mouse Click/Drag — Disabled
# ─────────────────────────────────────────────────────────────
# Agent sessions run non-interactively. Clicking or dragging in a pane
# can accidentally enter copy mode or send input to the agent. We keep
# mouse scroll (above) but disable everything else.
#
# Tip: In iTerm2, hold Option (Alt) to bypass tmux mouse and use
# native text selection when you need it.

unbind -n MouseDown1Pane
unbind -n MouseDown1Status
unbind -n MouseDrag1Border
unbind -n MouseDrag1Pane
unbind -n DoubleClick1Pane
unbind -n TripleClick1Pane

# ─────────────────────────────────────────────────────────────
# Key Bindings
# ─────────────────────────────────────────────────────────────

# Intuitive pane splitting (prefix + | and -)
bind | split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# Reload config with prefix + r
bind r source-file ~/.tmux.conf \; display "Config reloaded"

# ─────────────────────────────────────────────────────────────
# Status Bar
# ─────────────────────────────────────────────────────────────
# Layout: [session] window_num    ~/path | branch | CPU:N% RAM:N% HH:MM
# Status bar at top keeps the command prompt at the bottom where you expect it.

set -g status-position top
set -g status-style bg=default,fg=white
set -g status-interval 5

# Left: session name + window numbers
set -g status-left "#[fg=green][#S]#[fg=white] "
set -g status-left-length 30
setw -g window-status-format "#I"
setw -g window-status-current-format "#[fg=cyan,bold]#I#[fg=white,nobold]"

# Right: working dir | git branch | CPU/RAM | time
# (No model field — tmux can't see the agent's model, and Hermes renders its
#  own status bar; the retired external statusline.sh is gone.)
# Uses platform detection (macOS vs Linux) for system metrics
set -g status-right-length 100
set -g status-right "\
#[fg=cyan]#(echo '#{pane_current_path}' | sed \"s|$HOME|~|\")#[fg=white] | \
#[fg=yellow]#(cd '#{pane_current_path}' && git branch --show-current 2>/dev/null || echo '-')#[fg=white] | \
CPU:#(if [ $(uname) = Darwin ]; then top -l 1 -n 0 2>/dev/null | awk '/CPU usage/{print int($3)}'; else top -bn1 2>/dev/null | awk '/Cpu/{print int($2+$4)}'; fi)%% \
RAM:#(if [ $(uname) = Darwin ]; then memory_pressure 2>/dev/null | awk '/System-wide/{print int(100-$5)}'; else free 2>/dev/null | awk '/Mem:/{print int($3/$2*100)}'; fi)%% \
%H:%M"

# Active pane border
set -g pane-active-border-style fg=blue
